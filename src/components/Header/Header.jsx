import React from "react";
import "./Header.css"; // Optional: for styles, see below

const Header = () => {
  return (
    <header className="header">
      <h1 className="logo">FaceSmash</h1>
    </header>
  );
};

export default Header;
